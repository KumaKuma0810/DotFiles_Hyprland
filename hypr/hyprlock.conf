# ~/.config/hypr/hyprlock.conf

general {
    disable_loading_bar = false
    hide_cursor = true
    grace = 2
    no_fade_in = false
    blur_passes = 2
    blur_size = 8
    vibrancy = 0.15
    vibrancy_darkness = 0.0
    background_color = rgba(000000cc)
}

# Фон с блюром
background {
    monitor = eDP-1
    path = ""                # Захват экрана
    blur = true
    blur_size = 8
    blur_passes = 3
    noise = true
    noise_strength = 0.02
    brightness = 0.9
    contrast = 1.0
    vibrancy = 0.1
    vibrancy_darkness = 0.1
}

# 🌌 Наложение фоновой картинки (на весь экран)
image {
    monitor = eDP-1
    path = ~/.config/hypr/real_anime.jpg
    size = 1920, 1080         # ✅ Вручную подставь размер своего монитора
    position = 0, 0
    halign = center
    valign = center
    opacity = 0.8
    shadow = false
}
# 🔐 Поле ввода пароля
input-field {
    monitor = eDP-1
    size = 250, 50
    outline_thickness = 2
    outline_color = rgb(ff00ff)
    inner_color = rgba(000000aa)
    font_color = rgb(ffffff)
    placeholder_text = password 
    placeholder_color = rgb(888888)
    shadow_passes = 1
    fade_on_empty = false
    position = 0, -20
    halign = center
    valign = center
}

# 🕒 Часы
label {
    monitor = eDP-1
    text = cmd[update:1000] echo "$(date '+%H:%M')"
    color = rgb(ffffff)
    font_size = 64
    font_family = JetBrainsMono Nerd Font
    position = 0, 60
    halign = center
    valign = center
    shadow = true
}

# 👤 Имя пользователя
label {
    monitor = eDP-1
    text = $USER
    color = rgb(aaaaaa)
    font_size = 16
    font_family = JetBrainsMono Nerd Font
    position = 0, 110
    halign = center
    valign = center
}

